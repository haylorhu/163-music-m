.icon {
  width: 1em;
  height: 1em;
  vertical-align: -0.15em;
  fill: currentColor;
  overflow: hidden; }

* {
  margin: 0;
  padding: 0; }

* {
  box-sizing: border-box; }

*::after {
  box-sizing: border-box; }

*::before {
  box-sizing: border-box; }

ul, ol {
  list-style: none; }

a {
  color: inherit;
  text-decoration: none; }

input {
  font: inherit; }

body {
  font-size: 12px;
  line-height: 1.5; }

* {
  font-size: 20px; }

body header .logo {
  background: #d43c33;
  display: flex;
  align-content: center;
  flex-direction: row;
  color: white; }

body header .logo .leftWaper {
  flex-grow: 1;
  display: flex; }

body header .logo .WYlogo {
  height: 27px;
  width: 27px;
  margin: 22px 5px 22px 14px; }

body header .logo .logoText {
  margin: 19px 0px 18px 3px;
  display: inline-block; }

body header .logo .link {
  margin-right: 7px;
  border: 1px solid;
  border-radius: 22px;
  padding: 5px;
  font-size: 14px; }

body header .allTable {
  display: flex;
  justify-content: space-around;
  font-size: 1px;
  border-bottom: 1px solid #e6e6e6; }

body header .allTable .tabWaper {
  position: relative; }

body header .allTable .tabWaper.active::after {
  content: "";
  position: absolute;
  transform: translateX(-10%);
  height: 2px;
  width: 120%;
  background: #d43c33; }

body header .allTable p {
  font-size: 15px;
  padding: 10px 0px; }

body .page1 .topicWaper {
  position: relative; }

body .page1 .topicWaper .topic {
  margin: 10px;
  font-size: 20px; }

body .page1 .topicWaper .topic::after {
  content: '';
  position: absolute;
  left: 0px;
  top: 8px;
  height: 16px;
  width: 2px;
  background-color: #d43c33; }
